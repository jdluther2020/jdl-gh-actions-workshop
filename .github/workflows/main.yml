name: Hello World Training Workflow

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    env: 
      MY_ENV: "John David Luther"
    steps:
      - name: Greet the User
        run: echo "Hello World!"
      - name: Run a multi-line script
       run: |
          echo "Hello $MY_ENV"
          echo "Hello $GITHUB_ACTOR"
